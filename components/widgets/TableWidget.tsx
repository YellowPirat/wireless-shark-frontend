import {Card, CardHeader, CardTitle, CardContent} from '@/components/ui/card'
import {bgBlack} from "next/dist/lib/picocolors";

export default function TableWidget() {
    return (

                <span>Table Komponente</span>


)
}